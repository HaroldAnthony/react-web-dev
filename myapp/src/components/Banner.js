import React from "react";

const Banner = (props) => {
  return (                                                                    
    <div>
        <p>{props.heading.a}</p>
    </div>
  );
};

export default Banner;
