import React from "react";

const Banner = (props) => {
  return (                                                                    //PROP EXAMPLE 4 - BANNER
    <div>
        <p>{props.heading.a}</p>
      Banner
      <button>Click here</button> 
    </div>
  );
};

export default Banner;
